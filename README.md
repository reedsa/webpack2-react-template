# webpack2-react-template

Web App Template with Webpack 2 and React using Babel, PostCSS,
and dynamic index HTML generation.

Install dependencies with `npm i`

Build a distribution with `npm run build`
